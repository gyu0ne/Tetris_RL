[project]
name = "tetris-rl"
version = "0.1.0"
requires-python = "==3.13.*"
dependencies = [
    "numpy==2.3.2",
    "torch==2.8.0+cpu",
]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "SIM", "UP"]
