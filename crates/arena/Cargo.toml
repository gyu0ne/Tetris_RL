[package]
name = "arena"
version = "0.1.0"
description = "Placement-level heuristic teachers and imitation record generation"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
engine-core = { path = "../engine-core" }
rules-tetrio = { path = "../rules-tetrio" }
versus = { path = "../versus" }
flate2 = "1.1"
rayon = "1.11"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"

[lints]
workspace = true
